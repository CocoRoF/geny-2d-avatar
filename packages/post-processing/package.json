{
  "name": "@geny/post-processing",
  "version": "0.1.0",
  "private": true,
  "description": "docs/06 §4 Stage 1 alpha sanitation skeleton — RGBA8 버퍼의 premultiplied↔straight 변환 + 알파 노이즈 임계 정리 + tight bbox 계산. 세션 15 web-avatar 번들 텍스처 파이프라인에 선행한다. 세션 26: 결정론적 라운드트립 + golden sha256 으로 픽셀 단위 회귀.",
  "type": "module",
  "engines": {
    "node": ">=22.11.0"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "build:test": "tsc -p tsconfig.test.json",
    "clean": "rm -rf dist dist-test",
    "test": "pnpm build:test && node --test --test-reporter=spec 'dist-test/tests/**/*.test.js'"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.6.3"
  }
}
